/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["bhpdhlohcmaxorpuhhtb.supabase.co"], // Reemplaza con tu dominio de Supabase
  },
};

export default nextConfig;
